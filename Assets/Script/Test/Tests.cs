using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "Tests", menuName = "Scriptable Object/Tests", order = int.MaxValue)] //스크립터블 오브젝트 파일 이름, 메뉴(유니티 상단 assset / create 메뉴) 그리고 경로 이름 설정, 크리에이트 메뉴 중 어느 곳에 위치시킬 것 인지 순서 

public class Tests : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
